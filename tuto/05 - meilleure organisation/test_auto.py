"""
Ce fichier sert aux test automatisés.

C'est ce fichier qu'il faut modifier afin de générer les test.
On importe toutes les fonctions depuis le fichier "functions.py"
qui lui meme sert au fichier principal. 
Ainsi on utiliser les meme fonctions dans le main que dans les tests.

"""

import pytest

# Import de toutes les fonctions crées
from functions import *